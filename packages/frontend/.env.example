# UWAGA: NEXT_PUBLIC_* trafia do bundla przy `next build`, nie w runtime.
# W Dockerze przekaz je jako build arg, inaczej aplikacja bedzie strzelac do localhost.
NEXT_PUBLIC_API_URL=http://localhost:4000/api
NEXT_PUBLIC_MAPBOX_TOKEN=
